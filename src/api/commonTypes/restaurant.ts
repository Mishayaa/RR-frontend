export type Restaurant = {
    id: number,
    placeId:string,
    name: string,
    averageRating: number,
    posterUrl: string,
    formatted_address: string,
    open_now: boolean,
    serves_beer: boolean,
    isFavorite: boolean,
}
